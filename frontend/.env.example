# Optional: backend URL for the contact form (e.g. your Vercel serverless API or Node server).
# If unset, the form shows a message asking users to email you directly.
# Example: https://your-api.vercel.app
REACT_APP_BACKEND_URL=
